export class Scene{constructor(){this.children=[]}add(item){this.children.push(item)}}
export class PerspectiveCamera{constructor(){this.position={z:0};this.aspect=1}updateProjectionMatrix(){}}
export class MeshBasicMaterial{constructor(options={}){this.color=options.color||"#55c7ff"}}
export class TextGeometry{constructor(text){this.text=text}}
export class Mesh{constructor(geometry,material){this.geometry=geometry;this.material=material;this.position={x:0,y:0,z:0};this.rotation={x:0,y:0}}}
export class WebGLRenderer{constructor(options={}){this.canvas=options.canvas;this.ctx=this.canvas.getContext("2d")}setSize(w,h){this.canvas.width=Math.max(1,w*devicePixelRatio);this.canvas.height=Math.max(1,h*devicePixelRatio);this.ctx.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0)}render(scene){const ctx=this.ctx;const w=this.canvas.clientWidth;const h=this.canvas.clientHeight;ctx.clearRect(0,0,w,h);ctx.save();ctx.translate(w/2,h/2);scene.children.forEach((mesh,index)=>{const scale=1+(mesh.position.z||0)*.08;ctx.save();ctx.translate(mesh.position.x*90,mesh.position.y*-90);ctx.rotate(mesh.rotation.y*.45);ctx.scale(scale,scale);ctx.fillStyle=mesh.material.color;ctx.shadowColor="rgba(36,48,79,.18)";ctx.shadowBlur=22;ctx.beginPath();ctx.roundRect(-52,-52,104,104,24);ctx.fill();ctx.shadowBlur=0;ctx.fillStyle=index===2?"#24304f":"#fff";ctx.font="900 58px Fredoka, Arial";ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText(mesh.geometry.text,0,4);ctx.restore()});ctx.restore()}}
